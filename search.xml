<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>HashMap底层实现原理</title>
    <url>/2021/11/10/HashMap%E5%BA%95%E5%B1%82%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<h1 id="HashMap底层实现原理"><a href="#HashMap底层实现原理" class="headerlink" title="HashMap底层实现原理"></a>HashMap底层实现原理</h1><p>HashMap底层实现采用了哈希表，这是一种非常重要的数据结构，对于理解很多技术都非常有帮助(比如：redis数据库的核心技术和HashMap一样)。</p>
<p>数据结构中由数组和链表来实现对数据的存储，他们各有不同的特点。</p>
<p>(1) 数组：占用空间连续。 寻址容易，查询速度快。但增加和删除效率非常低。</p>
<p>(2) 链表：占用空间不连续。 寻址困难，查询速度慢。但增加和删除效率非常高。</p>
<p>哈希表的本质就是“数组+链表”，结合了数组和链表的优点(即查询快，增删效率高)。</p>
<h2 id="HashMap基本结构"><a href="#HashMap基本结构" class="headerlink" title="HashMap基本结构"></a>HashMap基本结构</h2><p>HashMap源码有如下两个核心内容：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class HashMap&lt;K,V&gt; extends AbstractMap&lt;K,V&gt; </span><br><span class="line">   implements Map&lt;K,V&gt;, Cloneable, Serializable &#123;</span><br><span class="line">	/**</span><br><span class="line">     * The default initial capacity - MUST be a power of two.</span><br><span class="line">     * 核心数组默认初始化的大小为16（数组大小必须为2的整数幂）。</span><br><span class="line">     */</span><br><span class="line">    static final int DEFAULT_INITIAL_CAPACITY = 1 &lt;&lt; 4; // aka 16</span><br><span class="line">    </span><br><span class="line">    /**</span><br><span class="line">     * The load factor used when none specified in constructor.、</span><br><span class="line">     * 负载因子（核心数组被占用超过0.75则自动开始扩容）。</span><br><span class="line">     */</span><br><span class="line">    static final float DEFAULT_LOAD_FACTOR = 0.75f;</span><br><span class="line">    </span><br><span class="line">    /**</span><br><span class="line">     * The table, initialized on first use, and resized as</span><br><span class="line">     * necessary. When allocated, length is always a power of two.</span><br><span class="line">     * (We also tolerate length zero in some operations to allow</span><br><span class="line">     * bootstrapping mechanics that are currently not needed.)</span><br><span class="line">     * 核心数组（根据需要可以扩容）。数组长度必须始终为2的整数幂。</span><br><span class="line">     */</span><br><span class="line">    transient Node&lt;K,V&gt;[] table;</span><br><span class="line">    //以下代码省略</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中的Node&lt;K,V&gt;[] table 就是HashMap的核心数组结构，也称之为“位桶数组”。Node&lt;K,V&gt;源码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static class Node&lt;K,V&gt; implements Map.Entry&lt;K,V&gt; &#123;</span><br><span class="line">    final int hash;</span><br><span class="line">    final K key;</span><br><span class="line">    V value;</span><br><span class="line">    Node&lt;K,V&gt; next;</span><br><span class="line"></span><br><span class="line">    Node(int hash, K key, V value, Node&lt;K,V&gt; next) &#123;</span><br><span class="line">        this.hash = hash;</span><br><span class="line">        this.key = key;</span><br><span class="line">        this.value = value;</span><br><span class="line">        this.next = next;</span><br><span class="line">    &#125;</span><br><span class="line">    //以下代码省略</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一个Node&lt;K,V&gt;对象存储了：</p>
<p>(1)key：键对象  value：值对象</p>
<p>(2)next：下一个节点</p>
<p>(3)hash：键对象的hash值</p>
<p>每一个Node&lt;K,V&gt;对象都是一个单向链表结构，如下表示一个Node&lt;K,V&gt;对象的典型示意：</p>
<p><img src="https://cdn.jsdelivr.net/gh/senleima/imagebed/img/Node%E5%AF%B9%E8%B1%A1%E5%AD%98%E5%82%A8%E7%BB%93%E6%9E%84.png" alt="Node对象存储结构"></p>
<p>如下为Node&lt;K,V&gt;[]数组的结构(也是HashMap的结构)：</p>
<p><img src="https://cdn.jsdelivr.net/gh/senleima/imagebed/img/Node%E6%95%B0%E7%BB%84%E5%AD%98%E5%82%A8%E7%BB%93%E6%9E%84.png" alt="Node数组存储结构"></p>
<h2 id="存储数据过程put-key-value"><a href="#存储数据过程put-key-value" class="headerlink" title="存储数据过程put(key,value)"></a><strong>存储数据过程put(key,value)</strong></h2><p>HashMap存储数据的核心是如何产生hash值，该值用来对应数组的存储位置。</p>
<p><img src="https://cdn.jsdelivr.net/gh/senleima/imagebed/img/HashMap%E5%AD%98%E5%82%A8%E6%95%B0%E6%8D%AE%E8%BF%87%E7%A8%8B.png" alt="HashMap存储数据过程"></p>
<p>我们的目的是将”key-value两个对象”成对存放到HashMap的Node&lt;K,V&gt;[]数组中。步骤如下：</p>
<p>(1) 获得key对象的hashcode</p>
<p>​      首先调用key对象的hashcode()方法，获得hashcode。</p>
<p>(2) 根据hashcode计算出hash值(要求在[0, 数组长度-1]区间)</p>
<p>​      hashcode是一个整数，需要将它转化成[0, 数组长度-1]的范围。我们要求转化后的hash值尽量均匀地分布在[0,数组长度-1]这个区间，减少“hash冲突”。</p>
<p>​      i. 一种极端简单和低下的算法是：</p>
<p>​      hash值 = hashcode/hashcode;</p>
<p>​      也就是说hash值总是1。意味着键值对对象都会存储到数组索引1位置，这样就形成一个非常长的链表。相当于每存储一个对象都会发生“hash冲突”，HashMap也退化成了一个“链表”。</p>
<p>​      ii. 一种简单和常用的算法是(相除取余算法)：</p>
<p>​      hash值 = hashcode%数组长度</p>
<p>这种算法可以让hash值均匀地分布在[0,数组长度-1]的区间。 早期的HashTable就是采用这种算法。但是，这种算法由于使用了“除法”，效率低下。JDK后来改进了算法。首先约定数组长度必须为2的整数幂，这样采用位运算即可实现取余的效果：hash值 = hashcode&amp;(数组长度-1)。</p>
<p>​      iii. 如下为自己测试简单的hash算法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Test &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        int h = 25760399;</span><br><span class="line">        int length = 16;//length为2的整数次幂,则h&amp;(length-1)相当于对length取模</span><br><span class="line">        System.out.println(myHash(h, length));</span><br><span class="line">    &#125;</span><br><span class="line">    /**</span><br><span class="line">     * @param h  任意整数</span><br><span class="line">     * @param length 长度必须为2的整数幂</span><br><span class="line">     * @return</span><br><span class="line">     */</span><br><span class="line">    public static int myHash(int h,int length)&#123;</span><br><span class="line">        System.out.println(h&amp;(length-1));</span><br><span class="line">        //length为2的整数幂情况下，和取余的值一样</span><br><span class="line">        System.out.println(h%length);//取余数</span><br><span class="line">        return h&amp;(length-1);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行如上程序，就能发现直接取余(h%length)和位运算(h&amp;(length-1))结果是一致的。事实上，为了获得更好的散列效果，JDK对hashcode进行了两次散列处理(核心目标就是为了分布更散更均匀)。</p>
<p>(3) 生成Node&lt;K,V&gt;对象</p>
<p>​     如上所述，一个Node&lt;K,V&gt;对象包含4部分：key对象、value对象、hash值、指向下一个Node&lt;K,V&gt;对象的引用。我们现在算出了hash值。下一个Node&lt;K,V&gt;对象的引用为null。</p>
<p>(4) 将Node&lt;K,V&gt;对象放到table数组中</p>
<p>​     如果本Node&lt;K,V&gt;对象对应的数组索引位置还没有放Node&lt;K,V&gt;对象，则直接将Node&lt;K,V&gt;对象存储进数组;如果对应索引位置已经有Node&lt;K,V&gt;对象，则将已有Node&lt;K,V&gt;对象的next指向本Node&lt;K,V&gt;对象，形成链表。</p>
<h3 id="总结如上过程"><a href="#总结如上过程" class="headerlink" title="总结如上过程"></a><strong>总结如上过程</strong></h3><p>   当添加一个元素(key-value)时，首先计算key的hash值，以此确定插入数组中的位置，但可能存在同一hash值的元素已经被放在数组同一位置了，这时就添加到同一hash值的元素的后面，他们在数组的同一位置，就形成了链表，同一个链表上的hash值是相同的，所以说数组存放的是链表。 JDK8中，当链表长度大于8时，链表就转换为红黑树，大大提高了查找的效率。</p>
<h2 id="取数据过程get-key"><a href="#取数据过程get-key" class="headerlink" title="取数据过程get(key)"></a>取数据过程get(key)</h2><p>   我们需要通过key对象获得“键值对”对象，进而返回value对象。步骤如下：</p>
<p>   (1) 获得key的hashcode，通过hash()散列算法得到hash值，进而定位到数组的位置。</p>
<p>   (2) 在链表上挨个比较key对象。 调用equals()方法，将key对象和链表上所有节点的key对象进行比较，直到碰到返回true的节点对象为止。</p>
<p>   (3) 返回equals()为true的节点对象的value对象。</p>
<p>   明白了存取数据的过程，下面再看下hashcode()和equals方法的关系：</p>
<p>   Java中规定，两个内容相同(equals()为true)的对象必须具有相等的hashcode。因为如果equals()为true而两个对象的hashcode不同，那在整个存储过程中就发生了悖论。</p>
<h2 id="扩容问题"><a href="#扩容问题" class="headerlink" title="扩容问题"></a><strong>扩容问题</strong></h2><p>   HashMap的位桶数组，初始大小为16。实际使用时大小是可变的。如果位桶数组中的元素达到(0.75*数组 length)， 则重新调整数组大小变为原来2倍大小。</p>
<p>   扩容很耗时，扩容的本质是定义新的更大的数组，并将旧数组内容挨个拷贝到新数组中。</p>
<h2 id="JDK8将链表在大于8情况下变为红黑二叉树"><a href="#JDK8将链表在大于8情况下变为红黑二叉树" class="headerlink" title="JDK8将链表在大于8情况下变为红黑二叉树"></a><strong>JDK8将链表在大于8情况下变为红黑二叉树</strong></h2><p>   JDK8中，HashMap在存储一个元素时，当对应链表长度大于8时，链表就转换为红黑树，这样又大大提高了查找的效率。</p>
]]></content>
      <categories>
        <category>Java基础</category>
      </categories>
      <tags>
        <tag>HashMap</tag>
      </tags>
  </entry>
  <entry>
    <title>自定义一个ArrayList</title>
    <url>/2021/11/07/%E8%87%AA%E5%AE%9A%E4%B9%89%E4%B8%80%E4%B8%AAArrayList/</url>
    <content><![CDATA[<h1 id="自定义一个ArrayList"><a href="#自定义一个ArrayList" class="headerlink" title="自定义一个ArrayList"></a>自定义一个ArrayList</h1><h2 id="ArrayList特点和底层实现"><a href="#ArrayList特点和底层实现" class="headerlink" title="ArrayList特点和底层实现"></a>ArrayList特点和底层实现</h2><p>ArrayList底层是用数组实现的存储。 特点：查询效率高，增删效率低，线程不安全。查看源码：</p>
<p><img src="https://cdn.jsdelivr.net/gh/senleima/imagebed/img/202111071028176.png" alt="自定义一个ArrayList1"></p>
<p>可以看出ArrayList底层使用Object数组来存储元素数据。所有的方法，都围绕这个核心的Object数组来开展。</p>
<p>我们知道，数组长度是有限的，而ArrayList是可以存放任意数量的对象，长度不受限制，那么它是怎么实现的呢?本质上就是通过定义新的更大的数组，将旧数组中的内容拷贝到新数组，来实现扩容。 ArrayList的Object数组初始化长度为10，如果我们存储满了这个数组，需要存储第11个对象，就会定义新的长度更大的数组，并将原数组内容和新的元素一起加入到新数组中，源码如下：</p>
<p><img src="https://cdn.jsdelivr.net/gh/senleima/imagebed/img/202111071030809.png" alt="自定义一个ArrayList2"></p>
<h2 id="自定义一个ArrayList，体会底层原理"><a href="#自定义一个ArrayList，体会底层原理" class="headerlink" title="自定义一个ArrayList，体会底层原理"></a>自定义一个ArrayList，体会底层原理</h2><p>代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">package com.msl.mycollection;</span><br><span class="line"></span><br><span class="line">import javax.management.RuntimeErrorException;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 自定义实现一个ArrayList,体会底层原理</span><br><span class="line"> * @author Senley</span><br><span class="line"> *</span><br><span class="line"> */</span><br><span class="line">public class MslArrayList&lt;E&gt; &#123;</span><br><span class="line">	</span><br><span class="line">	private Object[] elementData;</span><br><span class="line">	private int size;</span><br><span class="line">	private static final int DEFALT_CAPACITY = 10;</span><br><span class="line">	</span><br><span class="line">	public MslArrayList() &#123;</span><br><span class="line">		elementData = new Object[DEFALT_CAPACITY];</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public MslArrayList(int capacity) &#123;</span><br><span class="line">		if(capacity&lt;0) &#123;</span><br><span class="line">			throw new RuntimeException(&quot;容器的容量不能为负数&quot;);</span><br><span class="line">		&#125;else if(capacity==0) &#123;</span><br><span class="line">			elementData = new Object[DEFALT_CAPACITY];</span><br><span class="line">		&#125;else &#123;</span><br><span class="line">			elementData = new Object[capacity];</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public int size() &#123;</span><br><span class="line">		return size;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public boolean isEmpty() &#123;</span><br><span class="line">		return size==0?true:false;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public void add(E element) &#123;</span><br><span class="line">		//什么时候扩容</span><br><span class="line">		if(size == elementData.length) &#123;</span><br><span class="line">			//扩容操作</span><br><span class="line">			Object[] newArray = new Object[elementData.length + (elementData.length&gt;&gt;1)];//10--&gt;10+10/2</span><br><span class="line">			System.arraycopy(elementData, 0, newArray, 0, elementData.length);</span><br><span class="line">			elementData = newArray;		</span><br><span class="line">		&#125;</span><br><span class="line">		elementData[size++] = element;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public E get(int index) &#123;</span><br><span class="line">		checkRange(index);</span><br><span class="line">		return (E) elementData[index];</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public void set(E element,int index) &#123;</span><br><span class="line">		checkRange(index);</span><br><span class="line">		elementData[index] = element;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public void checkRange(int index) &#123;</span><br><span class="line">		//索引合法判断[0,size)</span><br><span class="line">		if(index&lt;0 || index&gt;size-1) &#123;</span><br><span class="line">			//不合法</span><br><span class="line">			throw new RuntimeException(&quot;索引不合法:&quot;+index);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public void remove(E element) &#123;</span><br><span class="line">		//将element和所有元素挨个比较,获得第一个比较为true的,返回.</span><br><span class="line">		for(int i=0;i&lt;size;i++) &#123;</span><br><span class="line">			if(element.equals(get(i))) &#123;//容器中所有的比较操作都是用的equals而不是==</span><br><span class="line">				//将该元素从此处移除</span><br><span class="line">				remove(i);</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public void remove(int index) &#123;</span><br><span class="line">		int numMoved = elementData.length-index-1;</span><br><span class="line">		if(numMoved&gt;0) &#123;</span><br><span class="line">			System.arraycopy(elementData, index+1, elementData, index, elementData.length-index-1);</span><br><span class="line">		&#125;</span><br><span class="line">		elementData[--size] = null;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	@Override</span><br><span class="line">	public String toString() &#123;</span><br><span class="line">		StringBuilder sb = new StringBuilder();</span><br><span class="line">		sb.append(&quot;[&quot;);</span><br><span class="line">		for(int i=0;i&lt;size;i++) &#123;</span><br><span class="line">			sb.append(elementData[i]+&quot;,&quot;);</span><br><span class="line">		&#125;</span><br><span class="line">		sb.setCharAt(sb.length()-1, &#x27;]&#x27;);</span><br><span class="line">		return sb.toString();</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public static void main(String[] args) &#123;</span><br><span class="line">		MslArrayList m = new MslArrayList(20);</span><br><span class="line">		for(int i=0;i&lt;40;i++) &#123;</span><br><span class="line">			m.add(&quot;senley&quot;+i);</span><br><span class="line">		&#125;</span><br><span class="line">		System.out.println(m);</span><br><span class="line">		m.set(&quot;test&quot;, 10);</span><br><span class="line">		System.out.println(m.get(10));</span><br><span class="line">		m.remove(3);</span><br><span class="line">		System.out.println(m);</span><br><span class="line">		m.remove(&quot;test&quot;);</span><br><span class="line">		System.out.println(m);</span><br><span class="line">		System.out.println(m.size);</span><br><span class="line">		System.out.println(m.isEmpty());</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>结果如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[senley0,senley1,senley2,senley3,senley4,senley5,senley6,senley7,senley8,senley9,senley10,senley11,senley12,senley13,senley14,senley15,senley16,senley17,senley18,senley19,senley20,senley21,senley22,senley23,senley24,senley25,senley26,senley27,senley28,senley29,senley30,senley31,senley32,senley33,senley34,senley35,senley36,senley37,senley38,senley39]</span><br><span class="line">test</span><br><span class="line">[senley0,senley1,senley2,senley4,senley5,senley6,senley7,senley8,senley9,test,senley11,senley12,senley13,senley14,senley15,senley16,senley17,senley18,senley19,senley20,senley21,senley22,senley23,senley24,senley25,senley26,senley27,senley28,senley29,senley30,senley31,senley32,senley33,senley34,senley35,senley36,senley37,senley38,senley39]</span><br><span class="line">[senley0,senley1,senley2,senley4,senley5,senley6,senley7,senley8,senley9,senley11,senley12,senley13,senley14,senley15,senley16,senley17,senley18,senley19,senley20,senley21,senley22,senley23,senley24,senley25,senley26,senley27,senley28,senley29,senley30,senley31,senley32,senley33,senley34,senley35,senley36,senley37,senley38,senley39]</span><br><span class="line">38</span><br><span class="line">false</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Java基础</category>
      </categories>
      <tags>
        <tag>ArrayList</tag>
      </tags>
  </entry>
  <entry>
    <title>自定义一个LinkedList</title>
    <url>/2021/11/08/%E8%87%AA%E5%AE%9A%E4%B9%89%E4%B8%80%E4%B8%AALinkedList/</url>
    <content><![CDATA[<h1 id="自定义一个LinkedList"><a href="#自定义一个LinkedList" class="headerlink" title="自定义一个LinkedList"></a>自定义一个LinkedList</h1><h2 id="LinkedList特点和底层实现"><a href="#LinkedList特点和底层实现" class="headerlink" title="LinkedList特点和底层实现"></a>LinkedList特点和底层实现</h2><p>LinkedList底层用双向链表实现的存储。特点：查询效率低，增删效率高，线程不安全。</p>
<p>双向链表也叫双链表，是链表的一种，它的每个数据节点中都有两个指针，分别指向前一个节点和后一个节点。 所以，从双向链表中的任意一个节点开始，都可以很方便地找到所有节点。下图为LinkedList的存储结构图：</p>
<p><img src="https://cdn.jsdelivr.net/gh/senleima/imagebed/img/%E8%87%AA%E5%AE%9A%E4%B9%89%E4%B8%80%E4%B8%AA%E9%93%BE%E8%A1%A81.png" alt="自定义一个链表1"></p>
<p>每个节点都应该有3部分内容：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class  Node &#123;</span><br><span class="line">        Node  previous;     //前一个节点</span><br><span class="line">        Object  element;    //本节点保存的数据</span><br><span class="line">        Node  next;         //后一个节点</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>查看LinkedList的源码，可以看到里面包含了双向链表的相关代码：</p>
<p><img src="https://cdn.jsdelivr.net/gh/senleima/imagebed/img/%E8%87%AA%E5%AE%9A%E4%B9%89%E4%B8%80%E4%B8%AA%E9%93%BE%E8%A1%A82.png" alt="自定义一个链表2"></p>
<h2 id="自定义一个LinkedList，体会底层原理"><a href="#自定义一个LinkedList，体会底层原理" class="headerlink" title="自定义一个LinkedList，体会底层原理"></a>自定义一个LinkedList，体会底层原理</h2><p>代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">package com.msl.mycollection;</span><br><span class="line">/**</span><br><span class="line"> * 自定义实现一个链表</span><br><span class="line"> * @author Senley</span><br><span class="line"> */</span><br><span class="line">public class MslLinkedList&lt;E&gt; &#123;</span><br><span class="line">	</span><br><span class="line">	private Node first;</span><br><span class="line">	private Node last;</span><br><span class="line">	private int size;</span><br><span class="line">	</span><br><span class="line">	public static void main(String[] args) &#123;</span><br><span class="line">		MslLinkedList&lt;String&gt; list = new MslLinkedList();</span><br><span class="line">		list.add(&quot;a&quot;);</span><br><span class="line">		list.add(&quot;b&quot;);</span><br><span class="line">		list.add(&quot;c&quot;);</span><br><span class="line">		System.out.println(list);</span><br><span class="line">		System.out.println(list.get(2));</span><br><span class="line">		list.remove(0);</span><br><span class="line">		System.out.println(list);</span><br><span class="line">		list.add(1, &quot;插入&quot;);</span><br><span class="line">		System.out.println(list);</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public void add(int index,E element) &#123;</span><br><span class="line">		checkRange(index);</span><br><span class="line">		Node newNode = new Node(element);</span><br><span class="line">		Node temp = getNode(index);</span><br><span class="line">		if(temp!=null) &#123;</span><br><span class="line">			Node up = temp.previous;</span><br><span class="line">			up.next = newNode;</span><br><span class="line">			newNode.previous = up;</span><br><span class="line">			newNode.next = temp;</span><br><span class="line">			temp.previous = newNode;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public void remove(int index) &#123;</span><br><span class="line">		checkRange(index);</span><br><span class="line">		Node temp = getNode(index);</span><br><span class="line">		if(temp!=null) &#123;</span><br><span class="line">			Node up = temp.previous;</span><br><span class="line">			Node down = temp.next;</span><br><span class="line">			if(up!=null) &#123;</span><br><span class="line">				up.next = down;</span><br><span class="line">			&#125;</span><br><span class="line">			if(down!=null) &#123;</span><br><span class="line">				down.previous = up;</span><br><span class="line">			&#125;</span><br><span class="line">			//被删除的元素是第一个元素时</span><br><span class="line">			if(index==0) &#123;</span><br><span class="line">				first = down;</span><br><span class="line">			&#125;</span><br><span class="line">			//被删除的元素是最后一个元素时</span><br><span class="line">			if(index==size-1) &#123;</span><br><span class="line">				last = up;</span><br><span class="line">			&#125;</span><br><span class="line">			size--;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public void add(E element) &#123;</span><br><span class="line">		Node node = new Node(element);</span><br><span class="line">		if(first==null) &#123;</span><br><span class="line">//			node.previous = null;</span><br><span class="line">//			node.next = null;</span><br><span class="line">			first = node;</span><br><span class="line">			last = node;</span><br><span class="line">		&#125;else &#123;</span><br><span class="line">			node.previous = last;</span><br><span class="line">			node.next = null;</span><br><span class="line">			last.next = node;</span><br><span class="line">			last = node;</span><br><span class="line">		&#125;</span><br><span class="line">		size++;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	public E get(int index) &#123;</span><br><span class="line">		checkRange(index);</span><br><span class="line">		Node temp = getNode(index);</span><br><span class="line">		return temp!=null?(E)temp.element:null;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	private void checkRange(int index) &#123;</span><br><span class="line">		if(index&lt;0 || index&gt;size-1) &#123;</span><br><span class="line">			throw new RuntimeException(&quot;索引数字不合法:&quot;+index);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	private Node getNode(int index) &#123;</span><br><span class="line">		checkRange(index);</span><br><span class="line">		Node temp = null;</span><br><span class="line">		if(index&lt;=(size&gt;&gt;1)) &#123;//size&gt;&gt;1相当于除以2</span><br><span class="line">			temp = first;</span><br><span class="line">			for(int i=0;i&lt;index;i++) &#123;</span><br><span class="line">				temp = temp.next;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;else &#123;</span><br><span class="line">			temp = last;</span><br><span class="line">			for(int i=size-1;i&gt;index;i--) &#123;</span><br><span class="line">				temp = temp.previous;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125; </span><br><span class="line">		return temp;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	@Override</span><br><span class="line">	public String toString() &#123;</span><br><span class="line">		//[a b c] first = a, last = c</span><br><span class="line">		StringBuilder sb = new StringBuilder(&quot;[&quot;);</span><br><span class="line">		Node temp = first;</span><br><span class="line">		while(temp!=null) &#123;</span><br><span class="line">			sb.append(temp.element+&quot;,&quot;);</span><br><span class="line">			temp = temp.next;</span><br><span class="line">		&#125;</span><br><span class="line">		sb.setCharAt(sb.length()-1, &#x27;]&#x27;);</span><br><span class="line">		return sb.toString();</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>结果如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[a,b,c]</span><br><span class="line">c</span><br><span class="line">[b,c]</span><br><span class="line">[b,插入,c]</span><br><span class="line"></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Java基础</category>
      </categories>
      <tags>
        <tag>LinkedList</tag>
      </tags>
  </entry>
</search>
